<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GraphGram.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GraphGram"
    Shell.FlyoutBehavior="Disabled"
    Shell.TabBarBackgroundColor="{AppThemeBinding Light={StaticResource myOrange3}, Dark={StaticResource myDarkDarkBlue}}"
    Shell.TabBarForegroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource myOrange2}}">
    
    

    <TabBar>
        <ShellContent Title="Graph"
                      Icon="axes.png"
                      ContentTemplate="{DataTemplate local:MainPage}"
                      Route="MainPage" />
        <ShellContent Title="Analysis"
                      Icon="calc.png"
                      ContentTemplate="{DataTemplate local:Analysis}" />
        <ShellContent Title="Import Export"
                      Icon="imex.png"
                      ContentTemplate="{DataTemplate local:ImportExport}" />
        <ShellContent Title="About"
                      Icon="ay.png"
                      ContentTemplate="{DataTemplate local:AboutPage}" />
    </TabBar>

</Shell>
